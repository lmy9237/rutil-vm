/*
package com.itinfo.rutilvm.api.model.computing;

import lombok.Builder;
import lombok.Getter;
import lombok.ToString;

@Getter @ToString @Builder
public class VmExportVo {
    private String vmId;
    private String domainName;
    private boolean exclusive; //Use the `exclusive` parameter when the virtual machine should be exported even if another copy of it already exists in the export domain (override).
    private boolean discardSnapshot; // Use the `discard_snapshots` parameter when the virtual machine should be exported with all of its snapshots collapsed.
    private String hostName;
    private String directory;
    private String fileName;
}
*/
